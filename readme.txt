
1. install clang for nvim treesitter
2. if parser has two, please disable the another one(relate to treesitter parser). Make sure those will not conflict each other.
3. Should not use python 3.10.0 for LSP install, the default pip has some problem.

